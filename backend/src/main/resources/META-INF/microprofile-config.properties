<<<<<<< HEAD
# ---------------------------------------------------------------------------------
# SecureTeam Access - MicroProfile Config
# ---------------------------------------------------------------------------------
# SECURITY NOTE:
#   - Do NOT hardcode secrets in this file.
#   - Use environment variables (12-factor app).
#
# In development, docker-compose.dev.yml sets these env vars.

# Steganography / master password (used by EncryptionService)
com.secureteam.steganography.master-password=${SECRET_MASTER_PASSWORD}

# Crypto salt/pepper for key derivation
com.secureteam.encryption.salt=${SECRET_ENCRYPTION_SALT}
com.secureteam.encryption.pepper=${SECRET_ENCRYPTION_PEPPER}

# Optional: CORS allowed origin (used by CorsFilter if implemented)
secureteam.cors.allowed-origin=${CORS_ALLOWED_ORIGIN}
=======
# ===============================================
# 🛡️ SecureTeam Access - Security Configuration
# ===============================================
# CRITICAL: All sensitive values MUST be provided via:
# 1. Environment Variables (recommended for production)
# 2. HashiCorp Vault (recommended)
# 3. Kubernetes Secrets
# 
# NEVER hardcode secrets in this file or source code!
# ===============================================

# Encryption Service Settings
# ⚠️ REQUIRED: Must be set via environment variable
# Example: export com_secureteam_steganography_master_password="YourSecurePassword123!"
com.secureteam.steganography.master-password=${SECRET_MASTER_PASSWORD}

# ⚠️ REQUIRED: Must be set via environment variable
# Example: export com_secureteam_steganography_salt="YourSaltValue"
com.secureteam.steganography.salt=${SECRET_SALT}

# MinIO Service Settings
# ⚠️ REQUIRED: Must be set via environment variable
minio.url=${MINIO_URL:http://localhost:9000}
minio.access-key=${SECRET_MINIO_ACCESS_KEY}
minio.secret-key=${SECRET_MINIO_SECRET_KEY}

# PASETO Service Settings
# ⚠️ REQUIRED: Must be set via environment variable for production
# Keys can be generated via: PasetoService initialization (dev only)
# For production: Store in Vault or KeyStore
# com.secureteam.auth.paseto.public-key=${SECRET_PASETO_PUBLIC_KEY}
# com.secureteam.auth.paseto.private-key=${SECRET_PASETO_PRIVATE_KEY}

# Database Configuration
# ⚠️ REQUIRED: Must be set via environment variable
# Example: export DATABASE_URL="jdbc:postgresql://host:5432/secureteam"
# Example: export DATABASE_USER="secureteam_app"
# Example: export DATABASE_PASSWORD="SecurePassword123!"

# Redis Configuration
# ⚠️ REQUIRED: Must be set via environment variable
# Example: export REDIS_URL="redis://user:password@host:6379"

# Logging Configuration
# Options: TRACE, DEBUG, INFO, WARN, ERROR
logging.level.com.secureteam=INFO
logging.level.org.jboss.as=WARN
>>>>>>> origin/main
